apiVersion: v1
kind: Service
metadata:
  namespace: default
  name: bhumika_app
  labels:
    app: bhumika_app
spec:
  type: ClusterIP
  selector:
    app: bhumika_app
  ports:
    - protocol: TCP
      name: http
      port: 80
      targetPort: 5000
